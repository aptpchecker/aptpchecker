# Configuration file for running the proof generation benchmark (all properties).
general:
  enable_incomplete_verification: False
solver:
  batch_size: 500
  beta-crown:
    beta: false
    lr_beta: 0.0
bab:
  branching:
    method: kfsb
    reduceop: max
    candidates: 5
  initial_max_domains: 1
attack:
  pgd_order: skip
